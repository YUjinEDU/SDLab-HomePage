---
phase: 02-content-visibility
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/queries/publications.test.ts
  - frontend/src/lib/queries/projects.test.ts
  - frontend/src/actions/__tests__/professor-actions.test.ts
autonomous: true
requirements:
  - VIS-01
  - VIS-02

must_haves:
  truths:
    - "publications.test.ts asserts getPublications() applies .eq('is_public', true)"
    - "publications.test.ts asserts getPatents() applies .eq('is_public', true)"
    - "publications.test.ts asserts getAllPublications() does NOT apply is_public filter"
    - "projects.test.ts asserts getProjects() applies .eq('is_public', true)"
    - "projects.test.ts asserts getFeaturedProjects() applies .eq('is_public', true)"
    - "professor-actions.test.ts asserts revalidateTag('publications') called on updatePublication"
    - "professor-actions.test.ts asserts revalidateTag('projects') called on updateProject"
    - "All new tests fail (RED) before implementation plans run"
  artifacts:
    - path: "frontend/src/lib/queries/publications.test.ts"
      provides: "VIS-01 unit tests for publication/patent queries"
      exports: []
    - path: "frontend/src/lib/queries/projects.test.ts"
      provides: "VIS-01 unit tests for project queries"
      exports: []
    - path: "frontend/src/actions/__tests__/professor-actions.test.ts"
      provides: "VIS-02 assertions for revalidateTag calls"
      exports: []
  key_links:
    - from: "frontend/src/lib/queries/publications.test.ts"
      to: "frontend/src/lib/queries/publications.ts"
      via: "mock Supabase client, spy on .eq() calls"
      pattern: "vi\\.mock.*supabase-server"
    - from: "frontend/src/actions/__tests__/professor-actions.test.ts"
      to: "next/cache revalidateTag"
      via: "vi.mock('next/cache')"
      pattern: "revalidateTag"
---

<objective>
Create failing test files that define the expected behavior of the is_public filter (VIS-01) and revalidateTag cache invalidation (VIS-02). These tests drive the implementation in Plans 02 and 03.

Purpose: Tests go RED first — Plans 02 and 03 make them GREEN.
Output: Three test files with failing assertions.
</objective>

<execution_context>
@C:/Users/sdlab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sdlab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-visibility/02-RESEARCH.md
</context>

<feature>
  <name>VIS-01 and VIS-02 Test Scaffold</name>
  <files>
    frontend/src/lib/queries/publications.test.ts,
    frontend/src/lib/queries/projects.test.ts,
    frontend/src/actions/__tests__/professor-actions.test.ts
  </files>
  <behavior>
    VIS-01 — publications.test.ts:
    - getPublications() mock chain must have .eq called with ("is_public", true)
    - getPatents() mock chain must have .eq called with ("is_public", true)
    - getAllPublications() mock chain must NOT have .eq called with ("is_public", true)
    - getPublicationBySlug("some-slug") mock chain must have .eq called with ("is_public", true)
    - getFeaturedPublications() mock chain must have .eq called with ("is_public", true)

    VIS-01 — projects.test.ts:
    - getProjects() mock chain must have .eq called with ("is_public", true)
    - getFeaturedProjects() mock chain must have .eq called with ("is_public", true)
    - getProjectBySlug("some-slug") mock chain must have .eq called with ("is_public", true)
    - getProjectById("id") mock chain must NOT have .eq called with ("is_public", true)

    VIS-02 — professor-actions.test.ts (extend existing file):
    - updatePublication() must call revalidateTag("publications")
    - updateProject() must call revalidateTag("projects")
    - createPublication() must call revalidateTag("publications")
    - deletePublication() must call revalidateTag("publications")
    - createProject() must call revalidateTag("projects")
    - deleteProject() must call revalidateTag("projects")

  </behavior>
  <implementation>
    Mock strategy: `vi.mock("@/lib/db/supabase-server")` — return a chainable builder where each method (from, select, eq, neq, order, single) returns `this`, and the terminal call resolves with `{ data: [], error: null }`. Spy on `.eq` to capture call args.

    For `next/cache` mock: `vi.mock("next/cache", () => ({ revalidatePath: vi.fn(), revalidateTag: vi.fn() }))`.

    All tests in this plan MUST fail (RED) until Plans 02 and 03 implement the actual code. Run `pnpm --filter frontend test --run` after creating files to confirm red state.

  </implementation>
</feature>

<verification>
Run: `pnpm --filter frontend test --run`
Expected: New tests FAIL (red). Existing tests still pass.
If existing tests also fail, fix mock setup before proceeding.
</verification>

<success_criteria>

- publications.test.ts and projects.test.ts exist and fail with "expected .eq to be called with is_public filter" type errors
- professor-actions.test.ts extended tests fail with "revalidateTag not called" type errors
- Existing passing tests remain green
  </success_criteria>

<output>
After completion, create `.planning/phases/02-content-visibility/02-01-SUMMARY.md`
</output>
