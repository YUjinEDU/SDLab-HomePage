---
phase: 02-content-visibility
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - frontend/src/actions/publications.ts
  - frontend/src/actions/projects.ts
autonomous: true
requirements:
  - VIS-02

must_haves:
  truths:
    - "After professor toggles is_public on a publication, public /publications page shows change on next refresh"
    - "After professor toggles is_public on a patent, public /patents page shows change on next refresh"
    - "revalidateTag('publications') called in createPublication, updatePublication, deletePublication"
    - "revalidateTag('projects') called in createProject, updateProject, deleteProject"
    - "Existing revalidatePath calls are preserved alongside new revalidateTag calls"
  artifacts:
    - path: "frontend/src/actions/publications.ts"
      provides: "Write actions that invalidate 'publications' cache tag on every mutation"
      exports: ["createPublication", "updatePublication", "deletePublication"]
    - path: "frontend/src/actions/projects.ts"
      provides: "Write actions that invalidate 'projects' cache tag on every mutation"
      exports: ["createProject", "updateProject", "deleteProject"]
  key_links:
    - from: "frontend/src/actions/publications.ts"
      to: "next/cache revalidateTag"
      via: "revalidateTag('publications') after successful write"
      pattern: "revalidateTag.*publications"
    - from: "frontend/src/actions/projects.ts"
      to: "next/cache revalidateTag"
      via: "revalidateTag('projects') after successful write"
      pattern: "revalidateTag.*projects"
---

<objective>
Add `revalidateTag` calls to all write actions in `actions/publications.ts` and `actions/projects.ts` so that `unstable_cache` entries (created in Plan 02) are invalidated on every mutation.

Purpose: VIS-02 — professor portal changes must appear on public pages immediately after browser refresh.
Output: Write actions that call both `revalidatePath` (existing) and `revalidateTag` (new) after each mutation.
</objective>

<execution_context>
@C:/Users/sdlab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sdlab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-visibility/02-RESEARCH.md
@.planning/phases/02-content-visibility/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Pattern from research. Use exactly. -->

```typescript
// actions/publications.ts — after successful DB write:
import { revalidatePath, revalidateTag } from "next/cache";

revalidateTag("publications"); // VIS-02: invalidates unstable_cache
revalidatePath("/professor/publications");
revalidatePath("/professor/patents");

// actions/projects.ts — after successful DB write:
revalidateTag("projects"); // VIS-02: invalidates unstable_cache
revalidatePath("/professor/projects");
revalidatePath("/projects");
```

Note: `revalidatePath` does NOT invalidate `unstable_cache` entries. Both calls are required.
Note: `revalidateTag` and `revalidatePath` are imported from the same `"next/cache"` module.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add revalidateTag to publication and project write actions</name>
  <files>
    frontend/src/actions/publications.ts,
    frontend/src/actions/projects.ts
  </files>
  <behavior>
    - actions/publications.ts: createPublication, updatePublication, deletePublication each call revalidateTag("publications") after successful write, before or alongside existing revalidatePath calls
    - actions/projects.ts: createProject, updateProject, deleteProject each call revalidateTag("projects") after successful write
    - Existing revalidatePath() calls are preserved (not removed)
    - If revalidateTag is not yet in the import from "next/cache", add it to the existing import
  </behavior>
  <action>
    1. Read `frontend/src/actions/publications.ts`.
       - Find the import line `import { revalidatePath } from "next/cache"` and add `revalidateTag` to it.
       - In each of createPublication, updatePublication, deletePublication: after the successful DB write (before or after existing revalidatePath calls), add `revalidateTag("publications")`.

    2. Read `frontend/src/actions/projects.ts`.
       - Same pattern: add revalidateTag to import.
       - In each of createProject, updateProject, deleteProject: add `revalidateTag("projects")`.

    Placement: add `revalidateTag(...)` immediately after the DB write succeeds, grouped with existing `revalidatePath` calls. Do not add it inside error branches.

  </action>
  <verify>
    <automated>pnpm --filter frontend test --run src/actions/__tests__/professor-actions.test.ts</automated>
  </verify>
  <done>
    VIS-02 assertions in professor-actions.test.ts pass GREEN. revalidateTag called with correct tag in all 6 write actions (create/update/delete for publications and projects).
  </done>
</task>

</tasks>

<verification>
Full test suite green: `pnpm --filter frontend test --run`
TypeScript compiles: `pnpm --filter frontend tsc --noEmit`
</verification>

<success_criteria>

- `revalidateTag("publications")` called in createPublication, updatePublication, deletePublication
- `revalidateTag("projects")` called in createProject, updateProject, deleteProject
- All existing revalidatePath calls preserved
- professor-actions.test.ts VIS-02 assertions GREEN
- No TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/02-content-visibility/02-03-SUMMARY.md`
</output>
