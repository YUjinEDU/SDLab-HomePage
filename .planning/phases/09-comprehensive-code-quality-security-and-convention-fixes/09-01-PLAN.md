---
phase: 09-comprehensive-code-quality-security-and-convention-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/db/middleware.ts
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - frontend/next.config.ts
  - .gitignore
  - frontend/.gitignore
  - frontend/.env.example
  - frontend/src/lib/db/env.ts
  - frontend/src/lib/db/supabase-client.ts
  - frontend/src/lib/db/supabase-server.ts
  - frontend/src/lib/db/supabase-static.ts
autonomous: true
requirements:
  - CQ-01
  - CQ-07
  - CQ-08
  - CQ-11
  - CQ-24
  - CQ-25
  - CQ-26

must_haves:
  truths:
    - "When SUPABASE_URL is missing, middleware returns 503 instead of silently passing requests through"
    - "pnpm audit shows no high/critical Next.js CVEs"
    - "supabase/.temp/ is in .gitignore and will not be accidentally committed"
    - "Security headers (X-Frame-Options, X-Content-Type-Options, HSTS, Referrer-Policy) are set on all responses"
    - "Missing env vars produce a clear error message at startup instead of cryptic runtime failures"
  artifacts:
    - path: "frontend/src/lib/db/middleware.ts"
      provides: "503 error when Supabase URL missing"
      contains: "503"
    - path: "frontend/next.config.ts"
      provides: "Security headers configuration"
      contains: "X-Frame-Options"
    - path: ".gitignore"
      provides: "supabase/.temp/ exclusion"
      contains: "supabase/.temp"
    - path: "frontend/.env.example"
      provides: "Env var documentation"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
    - path: "frontend/src/lib/db/env.ts"
      provides: "Runtime env validation utility"
      exports: ["getSupabaseUrl", "getSupabaseAnonKey"]
  key_links:
    - from: "frontend/src/lib/db/supabase-client.ts"
      to: "frontend/src/lib/db/env.ts"
      via: "import validated env getter"
      pattern: "import.*env"
    - from: "frontend/src/lib/db/supabase-server.ts"
      to: "frontend/src/lib/db/env.ts"
      via: "import validated env getter"
      pattern: "import.*env"
---

<objective>
Fix all security-critical issues: middleware bypass, Next.js CVE, .gitignore gaps, security headers, env validation, and dependency hygiene.

Purpose: Eliminate the highest-risk vulnerabilities and configuration gaps found in the code review before other fixes proceed.
Output: Hardened middleware, upgraded Next.js, security headers, env validation utility, comprehensive .gitignore, .env.example
</objective>

<execution_context>
@C:/Users/sdlab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sdlab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/docs/auth-security-review.md
@frontend/docs/review-config-build-deploy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Middleware 503, Next.js upgrade, .gitignore, security headers, @types/three</name>
  <files>
    frontend/src/lib/db/middleware.ts,
    frontend/package.json,
    frontend/next.config.ts,
    .gitignore,
    frontend/.gitignore
  </files>
  <action>
1. In `frontend/src/lib/db/middleware.ts` lines 8-10: Replace the `NextResponse.next()` fallback with a 503 response when `supabaseUrl` is missing or malformed. Return `new NextResponse("Service Unavailable: Database configuration error", { status: 503 })`. Add a `console.error` with a descriptive message ("CRITICAL: NEXT_PUBLIC_SUPABASE_URL is missing or invalid").

2. Run `cd frontend && pnpm up next@latest eslint-config-next@latest` to upgrade Next.js from 16.1.6 to >=16.1.7 (CVE patches). Then run `pnpm audit` to confirm no high/critical vulnerabilities remain.

3. In `.gitignore` (root), add these patterns:

```
# Supabase local
supabase/.temp/
frontend/supabase/.temp/

# Binary documents
*.hwp
*.docx
*.pdf

# Local image assets
images/faces/
```

4. In `frontend/.gitignore`, add:

```
# Supabase local
supabase/.temp/
```

5. In `frontend/next.config.ts`, add a `headers()` function that applies to all routes (`source: "/(.*)"`) with these security headers:
   - `X-Frame-Options: DENY`
   - `X-Content-Type-Options: nosniff`
   - `Referrer-Policy: strict-origin-when-cross-origin`
   - `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload` (only useful in production but safe to always set)
   - `X-DNS-Prefetch-Control: on`

6. In `frontend/package.json`, move `@types/three` from `dependencies` to `devDependencies`.
   </action>
   <verify>
   <automated>cd C:/Users/sdlab/workspace/Homepage/frontend && pnpm audit --audit-level=high 2>&1 | head -5 && grep -c "supabase/.temp" ../. gitignore && grep "X-Frame-Options" next.config.ts && grep "503" src/lib/db/middleware.ts && node -e "const pkg=require('./package.json'); console.log('@types/three in devDeps:', !!pkg.devDependencies['@types/three'])"</automated>
   </verify>
   <done>Middleware returns 503 when Supabase URL is missing. Next.js upgraded past CVE. .gitignore covers supabase/.temp and binary files. Security headers configured. @types/three moved to devDependencies.</done>
   </task>

<task type="auto">
  <name>Task 2: Env validation utility and .env.example</name>
  <files>
    frontend/src/lib/db/env.ts,
    frontend/src/lib/db/supabase-client.ts,
    frontend/src/lib/db/supabase-server.ts,
    frontend/src/lib/db/supabase-static.ts,
    frontend/.env.example
  </files>
  <action>
1. Create `frontend/src/lib/db/env.ts` with two exported functions:
   - `getSupabaseUrl(): string` — reads `process.env.NEXT_PUBLIC_SUPABASE_URL`, throws a descriptive Error if missing or empty: "Missing NEXT_PUBLIC_SUPABASE_URL environment variable. See .env.example for required variables."
   - `getSupabaseAnonKey(): string` — reads `process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY`, throws similarly if missing.
   Both functions should cache the validated value after first call (module-level `let` variable) so the check runs once per process.

2. Update `frontend/src/lib/db/supabase-client.ts`: Replace `process.env.NEXT_PUBLIC_SUPABASE_URL!` and `process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!` with `getSupabaseUrl()` and `getSupabaseAnonKey()` from `./env`. Remove the non-null assertions.

3. Do the same for `frontend/src/lib/db/supabase-server.ts` — replace `!` assertions with validated getters.

4. Do the same for `frontend/src/lib/db/supabase-static.ts`.

5. Create `frontend/.env.example`:

```
# Supabase Configuration (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
```

  </action>
  <verify>
    <automated>cd C:/Users/sdlab/workspace/Homepage/frontend && grep -c "getSupabaseUrl\|getSupabaseAnonKey" src/lib/db/supabase-client.ts src/lib/db/supabase-server.ts src/lib/db/supabase-static.ts && test -f .env.example && echo "env.example exists"</automated>
  </verify>
  <done>All Supabase client files use validated env getters instead of non-null assertions. .env.example documents required variables. Missing env vars produce clear error messages.</done>
</task>

</tasks>

<verification>
1. `cd frontend && pnpm build` succeeds with no errors
2. `pnpm audit --audit-level=high` shows 0 high/critical vulnerabilities
3. `grep "supabase/.temp" ../.gitignore` finds the pattern
4. Security headers present in next.config.ts
5. No `process.env.NEXT_PUBLIC_SUPABASE_URL!` patterns remain in lib/db/ (non-null assertions replaced)
</verification>

<success_criteria>

- Middleware returns 503 when SUPABASE_URL is missing (not silent passthrough)
- Next.js >=16.1.7 installed, CVEs patched
- supabase/.temp/ will not be accidentally committed
- All HTTP responses include security headers
- Env var validation catches missing config at startup with descriptive errors
- @types/three is in devDependencies, not dependencies
- .env.example documents required environment variables
  </success_criteria>

<output>
After completion, create `.planning/phases/09-comprehensive-code-quality-security-and-convention-fixes/09-01-SUMMARY.md`
</output>
