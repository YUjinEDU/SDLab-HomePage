---
phase: 09-comprehensive-code-quality-security-and-convention-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/shared/CategoryBadge.tsx
  - frontend/src/components/shared/CopyButton.tsx
  - frontend/src/components/shared/ExternalLinkButton.tsx
  - frontend/src/components/shared/FilterBar.tsx
  - frontend/src/components/shared/FilterChips.tsx
  - frontend/src/components/shared/PageHero.tsx
  - frontend/src/components/shared/Pagination.tsx
  - frontend/src/components/shared/SearchInput.tsx
  - frontend/src/components/shared/SectionHeader.tsx
  - frontend/src/components/shared/StatCard.tsx
  - frontend/src/components/shared/StatusBadge.tsx
  - frontend/src/components/shared/TagBadge.tsx
  - frontend/src/app/globals.css
  - CLAUDE.md
  - frontend/src/components/home/HomeHeroSection.tsx
  - frontend/src/app/(professor)/professor/contact/ContactForm.tsx
  - frontend/src/app/(professor)/professor/members/MemberForm.tsx
  - frontend/src/app/(professor)/professor/news/NewsForm.tsx
  - frontend/src/app/(professor)/professor/projects/ProjectForm.tsx
  - frontend/src/app/(professor)/professor/publications/PublicationForm.tsx
  - frontend/src/app/(internal)/internal/profile/ProfileForm.tsx
autonomous: true
requirements:
  - CQ-12
  - CQ-13
  - CQ-14
  - CQ-15

must_haves:
  truths:
    - "Zero shared components use `interface` keyword — all use `type` per project convention"
    - "CLAUDE.md and globals.css agree on the primary color value"
    - "No hardcoded bg-[#hex] values in non-canvas production components"
    - "Professor/internal form components use design tokens (bg-surface, text-foreground, border-border, focus:border-primary) instead of raw Tailwind color classes"
  artifacts:
    - path: "frontend/src/components/shared/CategoryBadge.tsx"
      provides: "Convention-compliant props type"
      contains: "type CategoryBadgeProps"
    - path: "frontend/src/components/shared/FilterBar.tsx"
      provides: "Convention-compliant props type"
      contains: "type FilterBarProps"
    - path: "CLAUDE.md"
      provides: "Accurate primary color documentation"
      contains: "#059669"
    - path: "frontend/src/app/(professor)/professor/contact/ContactForm.tsx"
      provides: "Design-token-based form styling"
      contains: "focus:border-primary"
  key_links:
    - from: "frontend/src/components/shared/*.tsx"
      to: "project convention"
      via: "type keyword instead of interface"
      pattern: "type \\w+Props ="
    - from: "frontend/src/app/(professor)/professor/**/Form.tsx"
      to: "frontend/src/app/globals.css"
      via: "design token CSS variables"
      pattern: "border-primary|bg-surface|text-foreground"
---

<objective>
Fix all convention violations: replace `interface` with `type` in 12 shared components, reconcile the color token mismatch between CLAUDE.md and globals.css, eliminate hardcoded hex colors in production components, and replace raw Tailwind color classes with design tokens in professor/internal form components.

Purpose: Align the codebase with documented conventions so future development follows consistent patterns.
Output: 12 updated component files, reconciled color documentation, cleaned up hardcoded colors, 6 form files using design tokens.
</objective>

<execution_context>
@C:/Users/sdlab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sdlab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@frontend/docs/code-review-2026-03-23.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert interface to type in 12 shared components</name>
  <files>
    frontend/src/components/shared/CategoryBadge.tsx,
    frontend/src/components/shared/CopyButton.tsx,
    frontend/src/components/shared/ExternalLinkButton.tsx,
    frontend/src/components/shared/FilterBar.tsx,
    frontend/src/components/shared/FilterChips.tsx,
    frontend/src/components/shared/PageHero.tsx,
    frontend/src/components/shared/Pagination.tsx,
    frontend/src/components/shared/SearchInput.tsx,
    frontend/src/components/shared/SectionHeader.tsx,
    frontend/src/components/shared/StatCard.tsx,
    frontend/src/components/shared/StatusBadge.tsx,
    frontend/src/components/shared/TagBadge.tsx
  </files>
  <action>
In each of the 12 files listed, replace `interface XxxProps {` with `type XxxProps = {`. This is a mechanical replacement — the two forms are functionally equivalent in TypeScript for object shapes.

Pattern: `interface FooProps {` becomes `type FooProps = {`

Files and their specific interface names:

- CategoryBadge.tsx: `CategoryBadgeProps`
- CopyButton.tsx: `CopyButtonProps`
- ExternalLinkButton.tsx: `ExternalLinkButtonProps`
- FilterBar.tsx: `FilterBarProps`
- FilterChips.tsx: `FilterChipsProps`
- PageHero.tsx: `PageHeroProps`
- Pagination.tsx: `PaginationProps`
- SearchInput.tsx: `SearchInputProps`
- SectionHeader.tsx: `SectionHeaderProps`
- StatCard.tsx: `StatCardProps`
- StatusBadge.tsx: `StatusBadgeProps`
- TagBadge.tsx: `TagBadgeProps`

After conversion, verify there are zero `interface` declarations remaining in `components/shared/`. Do NOT change any other code in these files.
</action>
<verify>
<automated>cd C:/Users/sdlab/workspace/Homepage && grep -r "^interface " frontend/src/components/shared/ | wc -l</automated>
</verify>
<done>All 12 shared components use `type XxxProps = {...}` instead of `interface XxxProps {...}`. Zero `interface` declarations in components/shared/. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile color tokens and fix hardcoded hex values</name>
  <files>
    CLAUDE.md,
    frontend/src/components/home/HomeHeroSection.tsx
  </files>
  <action>
1. **Color token reconciliation**: The CSS `globals.css` uses `--primary: #059669` (emerald-600). CLAUDE.md documents `#2D6A4F` (forest green). Since the implementation has been using `#059669` throughout and it is already the established visual identity, update CLAUDE.md to reflect reality:
   - Find the line containing `#2D6A4F` in the Styling section
   - Replace `#2D6A4F` with `#059669`
   - Update the description from "포레스트 그린" to "에메랄드 그린(#059669)"

2. **Hardcoded hex cleanup**: In `frontend/src/components/home/HomeHeroSection.tsx`:
   - Replace `bg-[#fafdfb]` with `bg-surface` (the design token for light surface backgrounds)
   - If `bg-surface` doesn't exist as a Tailwind utility from the theme, use `bg-background` instead

Note: Canvas demo files (`canvas-demo/`, `canvas-demo2/`) are development prototypes — leave their hex values as-is since Canvas API cannot use CSS variables.
</action>
<verify>
<automated>cd C:/Users/sdlab/workspace/Homepage && grep "#2D6A4F" CLAUDE.md | wc -l && grep "bg-\[#" frontend/src/components/home/HomeHeroSection.tsx | wc -l</automated>
</verify>
<done>CLAUDE.md accurately reflects the actual primary color #059669. No hardcoded bg-[#hex] in HomeHeroSection. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Replace raw Tailwind colors with design tokens in professor/internal forms</name>
  <files>
    frontend/src/app/(professor)/professor/contact/ContactForm.tsx,
    frontend/src/app/(professor)/professor/members/MemberForm.tsx,
    frontend/src/app/(professor)/professor/news/NewsForm.tsx,
    frontend/src/app/(professor)/professor/projects/ProjectForm.tsx,
    frontend/src/app/(professor)/professor/publications/PublicationForm.tsx,
    frontend/src/app/(internal)/internal/profile/ProfileForm.tsx
  </files>
  <action>
In all 6 form components, replace raw Tailwind color classes with the design tokens defined in `globals.css`. This is a mechanical find-and-replace within className strings. Apply these substitutions:

**Focus/ring colors (green -> primary token):**

- `focus:border-green-500` -> `focus:border-primary`
- `focus:ring-green-500` -> `focus:ring-primary`
- `focus:border-emerald-500` -> `focus:border-primary`
- `focus:ring-emerald-500` -> `focus:ring-primary`

**Text colors (gray -> semantic tokens):**

- `text-gray-900` -> `text-foreground`
- `text-gray-700` -> `text-text-secondary`
- `text-gray-500` -> `text-text-secondary`

**Background colors (gray -> surface token):**

- `bg-gray-50` -> `bg-surface`

**Border colors (gray -> border token):**

- `border-gray-300` -> `border-border`
- `border-gray-200` -> `border-border`

**Success feedback (green -> primary-muted/primary-dark):**

- `bg-green-50` -> `bg-primary-muted`
- `text-green-700` -> `text-primary-dark`

**Error feedback (red) -- KEEP AS-IS:**

- `bg-red-50`, `text-red-700`, `border-red-300` etc. -- do NOT change. There is no red/error design token, and red is the universal error color. Leave all red classes unchanged.

**Button colors:**

- `bg-green-600` -> `bg-primary`
- `hover:bg-green-700` -> `hover:bg-primary-dark`
- `bg-emerald-600` -> `bg-primary`
- `hover:bg-emerald-700` -> `hover:bg-primary-dark`

Do NOT change layout, structure, or logic. Only replace color class names. After replacement, visually scan each file to ensure no raw green/gray Tailwind color classes remain (except red for errors and white/black which are not design-token candidates).
</action>
<verify>
<automated>cd C:/Users/sdlab/workspace/Homepage && grep -c "focus:border-green-500\|focus:ring-green-500\|border-gray-300" frontend/src/app/\(professor\)/professor/contact/ContactForm.tsx frontend/src/app/\(professor\)/professor/members/MemberForm.tsx frontend/src/app/\(professor\)/professor/news/NewsForm.tsx frontend/src/app/\(professor\)/professor/projects/ProjectForm.tsx frontend/src/app/\(professor\)/professor/publications/PublicationForm.tsx frontend/src/app/\(internal\)/internal/profile/ProfileForm.tsx</automated>
</verify>
<done>All 6 form components use design tokens (bg-surface, text-foreground, text-text-secondary, border-border, focus:border-primary, focus:ring-primary, bg-primary, bg-primary-muted) instead of raw Tailwind color classes. Red error classes preserved. Build passes.</done>
</task>

</tasks>

<verification>
1. `grep -r "^interface " frontend/src/components/shared/` returns 0 matches
2. `grep "#2D6A4F" CLAUDE.md` returns 0 matches
3. `grep "#059669" CLAUDE.md` returns 1 match
4. `grep -r "focus:border-green-500" frontend/src/app/\(professor\)/professor/*/` returns 0 matches
5. `cd frontend && pnpm build` succeeds
</verification>

<success_criteria>

- All 12 shared components use `type` not `interface` for props definitions
- CLAUDE.md primary color matches globals.css primary color (#059669)
- No hardcoded bg-[#hex] in production (non-demo) components
- All 6 professor/internal form components use design tokens instead of raw Tailwind colors
- Build succeeds with no TypeScript errors
  </success_criteria>

<output>
After completion, create `.planning/phases/09-comprehensive-code-quality-security-and-convention-fixes/09-02-SUMMARY.md`
</output>
