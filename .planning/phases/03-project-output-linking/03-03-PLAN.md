---
phase: 03-project-output-linking
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - frontend/src/components/projects/ProjectOutputsSection.tsx
  - frontend/src/components/shared/ProjectBacklink.tsx
  - frontend/src/app/(public)/[locale]/projects/[slug]/page.tsx
  - frontend/src/app/(public)/[locale]/projects/page.tsx
  - frontend/src/app/(public)/[locale]/publications/[slug]/page.tsx
  - frontend/src/app/(public)/[locale]/patents/[slug]/page.tsx
  - frontend/messages/ko.json
  - frontend/messages/en.json
autonomous: false
requirements:
  - LINK-02
  - LINK-03
  - MOB-02
must_haves:
  truths:
    - "Project detail page is accessible to unauthenticated visitors for is_public=true projects"
    - "is_public=false project slug returns 404 to unauthenticated visitors"
    - "Project detail page shows linked publications and patents in a ProjectOutputsSection"
    - "Publication detail page shows linked project(s) via ProjectBacklink with correct href"
    - "Patent detail page shows linked project(s) via ProjectBacklink with correct href"
    - "ProjectOutputsSection renders without horizontal overflow at 375px (MOB-02)"
    - "Visitor can navigate from project detail to linked publication and back"
  artifacts:
    - path: "frontend/src/components/projects/ProjectOutputsSection.tsx"
      provides: "Renders linked publications and patents in two sub-sections"
      exports: ["ProjectOutputsSection"]
    - path: "frontend/src/components/shared/ProjectBacklink.tsx"
      provides: "Renders linked project link(s) on publication/patent detail pages"
      exports: ["ProjectBacklink"]
    - path: "frontend/src/app/(public)/[locale]/projects/[slug]/page.tsx"
      provides: "Public project detail page — no AccessDenied gate"
      contains: "notFound()"
    - path: "frontend/src/app/(public)/[locale]/projects/page.tsx"
      provides: "Public project list page — no AccessDenied gate"
  key_links:
    - from: "projects/[slug]/page.tsx"
      to: "getProjectOutputs"
      via: "await getProjectOutputs(project.id)"
      pattern: "getProjectOutputs"
    - from: "ProjectOutputsSection"
      to: "PublicationCard / PatentCard"
      via: "renders cards for publications and patents separately"
      pattern: "ProjectOutputsSection"
    - from: "publications/[slug]/page.tsx"
      to: "ProjectBacklink"
      via: "<ProjectBacklink projects={relatedProjects} />"
      pattern: "ProjectBacklink"
    - from: "patents/[slug]/page.tsx"
      to: "ProjectBacklink"
      via: "<ProjectBacklink projects={relatedProjects} />"
      pattern: "ProjectBacklink"
---

<objective>
Extract cross-link UI into reusable components, wire into detail pages, and remove the AccessDenied gate from project pages.

Purpose: Visitors can see project outputs and navigate bidirectionally between projects and their publications/patents. Removes the authentication wall on project pages per user locked decisions.
Output: Two new components (`ProjectOutputsSection`, `ProjectBacklink`) wired into three detail pages. Project list and detail pages public. Patent detail hardcoded strings replaced with i18n keys.
</objective>

<execution_context>
@C:/Users/sdlab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sdlab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-project-output-linking/03-01-SUMMARY.md

@frontend/src/app/(public)/[locale]/projects/[slug]/page.tsx
@frontend/src/app/(public)/[locale]/projects/page.tsx
@frontend/src/app/(public)/[locale]/publications/[slug]/page.tsx
@frontend/src/app/(public)/[locale]/patents/[slug]/page.tsx
@frontend/messages/ko.json
@frontend/messages/en.json

<interfaces>
<!-- From 03-01-PLAN.md (getProjectOutputs) -->
From frontend/src/lib/queries/publications.ts:
```typescript
export const getProjectOutputs: (projectId: string) => Promise<Publication[]>
// Returns publications + patents linked to project via publication_projects join
// Filtered is_public=true, ordered by year desc
// Cached with tags: ["projects", "publications"]
```

From frontend/src/types/publication.ts (existing):

```typescript
// Publication type includes: id, title, slug, year, type, projectIds, ...
// type field: "journal" | "conference" | "patent" | ... (string literal union)
// Patents have type === "patent" (or type === "patent_registration")
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProjectOutputsSection and ProjectBacklink components</name>
  <files>
    frontend/src/components/projects/ProjectOutputsSection.tsx,
    frontend/src/components/shared/ProjectBacklink.tsx,
    frontend/messages/ko.json,
    frontend/messages/en.json
  </files>
  <action>
    **ProjectOutputsSection** (`components/projects/ProjectOutputsSection.tsx`):
    - Server Component (no "use client")
    - Props: `type Props = { outputs: Publication[]; t: (key: string) => string }`
    - Split outputs into two arrays: `publications = outputs.filter(o => o.type !== "patent")` and `patents = outputs.filter(o => o.type === "patent")`
    - Render two conditional sub-sections: "관련 논문" (if publications.length) and "관련 특허" (if patents.length)
    - Return null if outputs is empty
    - For each publication: link to `/publications/${pub.slug}`, show title + year using PublicationCard or inline card (match existing inline card style from the project detail page before extraction)
    - For each patent: link to `/patents/${pat.slug}`, show title + year
    - Add `min-w-0 w-full` to the card container for MOB-02 compliance
    - Use translation keys: `t("sectionRelatedPublications")` and `t("sectionRelatedPatents")`

    **ProjectBacklink** (`components/shared/ProjectBacklink.tsx`):
    - Server Component (no "use client")
    - Props: `type Props = { projects: Project[] }`
    - Return null if projects is empty
    - Section heading: "연계 과제" (translation key: `t("sectionRelatedProjects")` — but this component receives pre-translated label OR accepts `t` — check how publications/[slug]/page.tsx passes `t` to other sections and match that pattern)
    - For each project: `<Link href={`/projects/${project.slug}`}>` with project title
    - Style to match the existing inline related-projects section extracted from publications/[slug]/page.tsx lines 241-282

    **i18n keys** — add to both `ko.json` and `en.json` if not already present:
    - `sectionRelatedPublications`: ko: "관련 논문", en: "Related Publications"
    - `sectionRelatedPatents`: ko: "관련 특허", en: "Related Patents"
    - `sectionRelatedProjects`: ko: "연계 과제", en: "Related Projects" (already exists in publications page — check before adding)

    Use `type` keyword (not `interface`). No `enum`. Follow existing component patterns in the project.

  </action>
  <verify>
    <automated>cd frontend && pnpm tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Both component files exist and compile. i18n keys present in ko.json and en.json.</done>
</task>

<task type="auto">
  <name>Task 2: Remove AccessDenied gate from project pages and wire components into detail pages</name>
  <files>
    frontend/src/app/(public)/[locale]/projects/page.tsx,
    frontend/src/app/(public)/[locale]/projects/[slug]/page.tsx,
    frontend/src/app/(public)/[locale]/publications/[slug]/page.tsx,
    frontend/src/app/(public)/[locale]/patents/[slug]/page.tsx
  </files>
  <action>
    **projects/page.tsx (list page):**
    - Remove `getSession()` / `if (!user) return <AccessDenied />` guard entirely
    - The is_public filter on the query already handles visibility (from Phase 2, VIS-01)
    - Keep all other page logic unchanged

    **projects/[slug]/page.tsx (detail page):**
    - Remove `getSession()` / `if (!user) return <AccessDenied />` guard
    - The query `getProjectBySlug` already filters is_public via RLS (DB-02). If the project is not found (is_public=false or does not exist), `notFound()` is already called — verify this and add if missing
    - Replace the existing inline related publications section (lines ~268-292, which uses `getPublications().filter(...)`) with:
      ```tsx
      const outputs = await getProjectOutputs(project.id);
      // ... later in JSX:
      <ProjectOutputsSection outputs={outputs} t={t} />
      ```
    - Import `getProjectOutputs` from `@/lib/queries/publications`
    - Import `ProjectOutputsSection` from `@/components/projects/ProjectOutputsSection`
    - Add `min-w-0` to the outputs section wrapper for MOB-02

    **publications/[slug]/page.tsx:**
    - Replace inline related-projects section (lines ~241-282) with `<ProjectBacklink projects={relatedProjects} />`
    - `relatedProjects` is already fetched (the query already returns projectIds and joins project data — verify the existing data shape and pass accordingly)
    - Import `ProjectBacklink` from `@/components/shared/ProjectBacklink`
    - Only show is_public=true projects: the query already filters this per user locked decision (related projects shown = only is_public=true ones)

    **patents/[slug]/page.tsx:**
    - Same as publications/[slug]/page.tsx — replace inline related-projects section (lines ~185-226)
    - Also fix: replace hardcoded `"관련 프로젝트"` and `"연구 분야"` strings with `t("sectionRelatedProjects")` and `t("sectionResearchAreas")` keys (per pitfall 5 in research)
    - Add `sectionResearchAreas` to ko.json/en.json if not already present: ko: "연구 분야", en: "Research Areas"

    **Per user locked decisions:**
    - `/projects` list: no AccessDenied, is_public filter already in query
    - `/projects/[slug]`: no AccessDenied, is_public=false returns 404 via notFound()
    - Cross-linking only shows is_public=true projects (handled by query)

  </action>
  <verify>
    <automated>cd frontend && pnpm tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    All four pages compile. Project list and detail pages render for unauthenticated visitors. Detail pages show cross-linked outputs/backlinks. is_public=false project slug returns 404.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - ProjectOutputsSection on project detail pages
    - ProjectBacklink on publication and patent detail pages
    - AccessDenied gate removed from /projects and /projects/[slug]
    - Mobile overflow fixes on outputs section
  </what-built>
  <how-to-verify>
    1. Start dev server: `cd frontend && pnpm dev`
    2. Open a project detail page as a logged-out visitor — confirm it loads (no redirect/AccessDenied)
    3. If the project has linked publications, confirm they appear in "관련 논문" section
    4. Click a publication link — confirm it navigates to /publications/[slug]
    5. On the publication detail page, confirm "연계 과제" section appears with a link back to the project
    6. Repeat for a patent detail page
    7. Open /projects list as logged-out — confirm it loads
    8. In DevTools at 375px, open a project detail with outputs — confirm no horizontal overflow
    9. Try accessing a known is_public=false project slug directly — confirm 404
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
cd frontend && pnpm vitest run && pnpm tsc --noEmit
</verification>

<success_criteria>
Full test suite passes. TypeScript compiles cleanly. Unauthenticated visitors can browse /projects and project detail pages. Cross-links work bidirectionally. is_public=false projects return 404. No mobile overflow at 375px.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-output-linking/03-03-SUMMARY.md`
</output>
