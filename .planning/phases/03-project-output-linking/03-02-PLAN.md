---
phase: 03-project-output-linking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/publications/PublicationCard.tsx
  - frontend/src/components/patents/PatentCard.tsx
  - frontend/src/components/projects/ProjectCard.tsx
autonomous: true
requirements:
  - MOB-01
must_haves:
  truths:
    - "PublicationCard renders without horizontal overflow at 375px viewport"
    - "PatentCard renders without horizontal overflow at 375px viewport"
    - "ProjectCard renders without horizontal overflow at 375px viewport"
    - "Long Korean text in card titles wraps instead of overflowing"
  artifacts:
    - path: "frontend/src/components/publications/PublicationCard.tsx"
      provides: "Mobile-safe publication card"
      contains: "min-w-0"
    - path: "frontend/src/components/patents/PatentCard.tsx"
      provides: "Mobile-safe patent card"
      contains: "min-w-0"
    - path: "frontend/src/components/projects/ProjectCard.tsx"
      provides: "Mobile-safe project card"
      contains: "min-w-0"
  key_links:
    - from: "card text container div"
      to: "long Korean title text"
      via: "min-w-0 flex-1 on container, break-words on title"
      pattern: "min-w-0"
---

<objective>
Fix mobile layout overflow on PublicationCard, PatentCard, and ProjectCard at 375px viewport.

Purpose: Phase 3 success criterion requires cards render without horizontal overflow on mobile. Korean titles are long and push flex children past viewport width without `min-w-0` guards.
Output: Three updated card components with mobile-safe Tailwind classes.
</objective>

<execution_context>
@C:/Users/sdlab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sdlab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@frontend/src/components/publications/PublicationCard.tsx
@frontend/src/components/patents/PatentCard.tsx
@frontend/src/components/projects/ProjectCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PublicationCard and PatentCard mobile overflow</name>
  <files>frontend/src/components/publications/PublicationCard.tsx, frontend/src/components/patents/PatentCard.tsx</files>
  <action>
    For each card, locate the text container div that wraps the title and metadata. Apply the following pattern:

    1. On the flex container wrapping the text (the div that is a flex child), add `min-w-0` and ensure it has `flex-1` (already present per research).
    2. On the title `<p>` or `<h3>` element, add `break-words` Tailwind class.
    3. On any metadata line with potentially long text (venue, authors), add `truncate` or `break-words` as appropriate.

    Pattern to follow (from 03-RESEARCH.md):
    ```tsx
    <div className="min-w-0 flex-1">
      <p className="text-sm font-medium text-foreground leading-snug break-words">
        {title}
      </p>
    </div>
    ```

    Do NOT change card layout structure, colors, or spacing — only add the overflow-prevention classes.

  </action>
  <verify>
    <automated>cd frontend && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Both cards compile. When rendered at 375px in DevTools, no horizontal scrollbar appears even with a 60-character Korean title.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ProjectCard mobile overflow</name>
  <files>frontend/src/components/projects/ProjectCard.tsx</files>
  <action>
    Same pattern as Task 1. ProjectCard already uses `flex-col h-full` per research notes — add `min-w-0` to the text container div and `break-words` to the title element.

    Also check: if ProjectCard has a status badge or tag row that uses `flex flex-wrap`, ensure the row has `min-w-0` on the parent. Tags themselves should be `shrink-0` to prevent squishing.

    Do NOT change card layout structure, colors, or spacing.

  </action>
  <verify>
    <automated>cd frontend && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ProjectCard compiles. At 375px viewport no horizontal overflow. Tags wrap to next line rather than overflow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mobile overflow fixes applied to PublicationCard, PatentCard, and ProjectCard.</what-built>
  <how-to-verify>
    1. Start dev server: `cd frontend && pnpm dev`
    2. Open http://localhost:3000/publications in Chrome DevTools
    3. Set viewport to 375px (iPhone SE preset)
    4. Scroll through publication cards — confirm no horizontal scrollbar, long titles wrap
    5. Repeat at /patents and /projects
    6. Test with a very long title if possible (or temporarily inject one in JSX)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any overflow issues to fix</resume-signal>
</task>

</tasks>

<verification>
cd frontend && pnpm tsc --noEmit
</verification>

<success_criteria>
All three cards render without horizontal overflow at 375px. TypeScript compiles cleanly. No layout changes visible at desktop viewport.
</success_criteria>

<output>
After completion, create `.planning/phases/03-project-output-linking/03-02-SUMMARY.md`
</output>
