---
type: project
tags:
  - lab-website
  - ia
  - page-design
created: 2026-03-07
---

# 사이트 구조 및 페이지 설계

상위 문서: [[홈페이지 계획]]

이 문서의 역할:

- 공개 포털 중심으로 어떤 페이지를 만들고, 각 페이지를 어떤 컴포넌트로 나눌지 정의하는 화면 설계 문서

## 구현 상태 (Implementation Status)

> **공개 포털 MVP 구현 완료** — 원본 설계 대비 약 96% 일치

- 구현 완료: 7개 공개 페이지 + 4개 상세 페이지 + 글로벌 에러/로딩 상태
- Phase 2 예정: Board (`/board`), Live Demo (`/live-demo`)
- 추가 구현: Patents (`/patents`, `/patents/[slug]`) — 원본 설계에 없었으나 추가됨
- 구조 변경: Members 페이지를 `/members` (교수 프로필)와 `/members/students` (학생 목록)로 분리
- 스타일 변경: 포인트 색상 `#2D6A4F` → `#059669` (에메랄드 그린), shadcn/ui 미사용 (순수 Tailwind CSS v4)
- 네비게이션: 메가 메뉴가 아닌 개별 드롭다운 방식, Members에 하위 링크(교수, 학생) 포함
- 컴포넌트: 총 51개 `.tsx` 파일 구현 완료

## 사이트 구조

### 공개 영역

- `/` ✅
- `/members` — 교수 프로필 전용 ✅
- `/members/students` — 학생 목록 (그룹 탭: PhD, MS, 석박통합, UG, Alumni) ✅
- `/members/[slug]` — 멤버 상세 (논문/프로젝트 교차 링크) ✅
- `/research` ✅
- `/publications` ✅
- `/publications/[slug]` — 논문 상세 ✅
- `/projects` ✅
- `/projects/[slug]` — 프로젝트 상세 ✅
- `/patents` — 특허 목록 (Publication type="patent" 활용) ✅
- `/patents/[slug]` — 특허 상세 ✅
- `/contact` ✅
- `/board` — Phase 2 예정
- `/live-demo` — Phase 2 예정

에러/로딩 상태:

- `not-found.tsx`, `error.tsx`, `loading.tsx` (글로벌) ✅
- `publications/loading.tsx`, `projects/loading.tsx` ✅

### 인증 영역

- `/login`
- `/account`

## 공통 UX 원칙

- 상단 네비게이션은 전 페이지에서 동일하게 유지한다.
  - ✅ 구현: 개별 드롭다운 방식 (`MainNavigation`), 모바일은 아코디언 메뉴 (`MobileNavigationDrawer`)
  - ✅ Members 메뉴에 하위 링크(교수, 학생) 포함
- breadcrumb와 현재 메뉴 강조를 일관되게 적용한다.
- 검색, 필터, 태그, 페이지네이션은 공통 패턴으로 맞춘다. ✅
- 모바일 1열, 태블릿 2열, 데스크톱 3열 기준으로 설계한다. ✅

## 홈 `/` ✅

구현된 섹션 (8개):

- `HomeHeroSection` + `NetworkBackground` — Hero 영역 ✅
- `LabIntroSection` — 연구실 소개 (원본 설계에 없었으나 추가) ✅
- `ResearchAreasSection` — 연구 분야 ✅
- `FeaturedPublicationsSection` — 대표 논문 ✅
- `FeaturedProjectsSection` — 대표 프로젝트 ✅
- `MembersSnapshotSection` — 멤버 미리보기 ✅
- `LatestNewsSection` — 최근 소식 ✅
- `ContactSummarySection` — 연락처 요약 ✅

> 참고: `StatsBarSection`이 존재하지만 홈에서는 미사용 상태

필수 데이터:

- 연구실명
- 소개 문구
- 연구 분야
- 대표 논문
- 대표 프로젝트
- 최근 공지
- 연락처

## Members `/members` ✅

> 구현 시 구조 변경: 단일 페이지가 아닌 교수/학생 분리 구조로 구현됨

### `/members` — 교수 프로필 전용 ✅

- `ProfessorProfile` — 교수님 프로필 전체 (학력, 경력, 수상, 외부 링크)
- `MemberEducationTimeline` — 학력 타임라인
- `MemberCareerTimeline` — 경력 타임라인
- `MemberContactLinks` — 연락처 및 외부 링크

### `/members/students` — 학생 목록 ✅

- `MemberGroupTabs` (client) — 그룹 탭
  - `PhD`
  - `MS`
  - `석박통합`
  - `UG` (Undergraduate)
  - `Alumni`
- `MemberGrid` — 멤버 카드 그리드
- `MemberCard` — 개별 멤버 카드

### `/members/[slug]` — 멤버 상세 ✅

- 프로필 이미지
- 한글 / 영문 이름
- 직위 / 과정
- 소속
- 연구 키워드
- 이메일 / GitHub / Scholar / Homepage
- Education
- Career
- 관련 논문 / 프로젝트 교차 링크

## Research `/research` ✅

구성:

- Research Hero
- 3개 연구 분야 상세 카드 (`ResearchAreaDetailCard`) ✅
- 관련 콘텐츠 미리보기 (`RelatedContentPreview`) ✅

## Publications `/publications` ✅

구성:

- 제목 영역 ✅
- `PublicationFilters` (client) — 필터/검색 ✅
  - 연도
  - 타입
  - 학회/저널
  - 연구 분야
  - 저자명
- `PublicationExternalLinks` — 외부 링크 영역 ✅
  - Google Scholar
  - DBLP
  - ORCID
  - Scopus
- `PublicationStatsPanel` — 통계 요약 ✅
- `PublicationList` + `PublicationCard` — 목록 ✅
- `Pagination` — 페이지네이션 ✅

상세 페이지 `/publications/[slug]` ✅:

- 제목
- 저자
- 학회/저널 정보
- 초록
- 키워드
- BibTeX (`BibtexCopyButton`, client)
- 관련 프로젝트
- 관련 멤버

## Projects `/projects` ✅

구성:

- 상단 제목 + 요약 문구 ✅
- `ProjectFilters` (client) — 카테고리/상태 필터 ✅
- 엔트리 수 / 현재 필터 상태 ✅
- `ProjectGrid` + `ProjectCard` — 카드 그리드 ✅
- `Pagination` — 페이지네이션 ✅

프로젝트 카드:

- 썸네일
- 상태 배지 (`StatusBadge`) ✅
- 카테고리 배지 (`CategoryBadge`) ✅
- 제목
- 기간
- 기관
- 프로그램 유형
- 예산
- 핵심 설명
- 태그

상세 페이지 `/projects/[slug]` ✅:

- 프로젝트 개요
- 목표
- 기간
- 기관 / 지원처
- 참여 멤버
- 관련 논문
- 산출물
- 데모 링크
- 이미지 / 결과물

## Patents `/patents` ✅

> 원본 설계에 없었으나 구현 시 추가. Publication 모델에서 `type="patent"`인 항목을 별도 페이지로 분리.

구성:

- 상단 제목 영역
- 필터/검색 (Publications와 동일 패턴)
- `PatentCard` — 특허 카드 목록
- `Pagination` — 페이지네이션

상세 페이지 `/patents/[slug]` ✅:

- Publications 상세와 동일한 구조 (교차 링크 포함)

컴포넌트:

- `PatentCard` ✅

---

## Board `/board` — Phase 2 예정

구성:

- 상단 제목 영역
- 검색창
- 카테고리 필터
- 엔트리 수 표시
- Board 리스트

항목 구성:

- 카테고리 아이콘
- 제목
- 짧은 요약
- 본문 미리보기
- 날짜
- 관련 링크

상세 페이지 `/board/[slug]`:

- 제목
- 카테고리
- 작성일
- 본문
- 첨부 링크
- 관련 프로젝트 / 논문 / 행사

## Live Demo `/live-demo` — Phase 2 예정

구성:

- 상단 검색창
- 엔트리 수 표시
- 좌측 카테고리 메뉴
- 우측 데모 카드 그리드

데모 카드:

- 썸네일
- 제목
- 한 줄 설명
- 공개 여부 배지
- 외부 링크 또는 내부 진입 버튼

상세 페이지 `/live-demo/[slug]`:

- 데모 소개
- 사용 목적
- 관련 프로젝트
- 접근 조건
- 실행 링크
- 문서 링크

## Contact `/contact` ✅

구성:

- `ContactInfoCard` — 연구실 위치, 대표 이메일, 연락처 ✅
- `MapSection` — 지도 ✅
- `DirectionsSection` — 오시는 길 ✅

## 페이지별 컴포넌트 설계

이 섹션은 실제 구현 시 `app/`와 `components/` 구조로 바로 옮길 수 있도록, 페이지를 어떤 컴포넌트 단위로 쪼갤지 정리한 것이다.

기본 원칙:

- 페이지는 `섹션 컴포넌트`로 나눈다.
- 카드, 배지, 필터, 검색창은 가능한 한 공통 컴포넌트로 재사용한다.
- 리스트 페이지와 상세 페이지는 데이터 조회 방식만 다르고, 표현 컴포넌트는 최대한 공유한다.
- 내부 포털과 공개 페이지는 시각 스타일은 다르더라도 작은 UI 단위는 재사용 가능하게 설계한다.

## 공통 컴포넌트

### 레이아웃 계열 (5개 구현)

- `SiteHeader` ✅
- `SiteFooter` ✅
- `MainNavigation` — 개별 드롭다운 방식, Members 하위 링크 포함 ✅
- `MobileNavigationDrawer` — 아코디언 메뉴 ✅
- `Container` ✅
- ~~`Breadcrumbs`~~ — 미구현
- `PageHero` ✅ (shared에 배치)
- `SectionHeader` ✅ (shared에 배치)

### 탐색 / 상호작용 계열 (5개 구현)

- `SearchInput` ✅
- `FilterBar` ✅
- `FilterChips` ✅
- `Pagination` ✅
- `EmptyState` ✅
- ~~`SortDropdown`~~ — 미구현
- ~~`AccessDeniedState`~~ — 미구현 (Phase 2)

### 데이터 표시 계열 (6개 구현)

- `TagBadge` ✅
- `StatusBadge` ✅
- `CategoryBadge` ✅
- `StatCard` ✅
- `ExternalLinkButton` ✅
- `CopyButton` ✅
- ~~`InfoRow`~~ — 미구현

### 카드 계열

- `MemberCard` ✅
- `PublicationCard` ✅
- `ProjectCard` ✅
- `PatentCard` ✅ (추가됨)
- `BoardPostListItem` — Phase 2
- `DemoCard` — Phase 2
- ~~`ResearchAreaCard`~~ → `ResearchAreaDetailCard`로 변경 ✅

## 홈 `/` 컴포넌트 ✅

### 페이지 조합 (9개 구현)

- `HomeHeroSection` + `NetworkBackground` ✅
- `LabIntroSection` ✅ (추가됨 — 원본 설계에 없음)
- `ResearchAreasSection` ✅ (이름 변경: `FeaturedResearchSection` →)
- `FeaturedPublicationsSection` ✅ (이름 변경: `RecentPublicationsSection` →)
- `FeaturedProjectsSection` ✅
- `MembersSnapshotSection` ✅
- `LatestNewsSection` ✅
- `ContactSummarySection` ✅
- `StatsBarSection` — 존재하지만 홈에서 미사용

### 세부 컴포넌트

- `PublicationCard` ✅
- `ProjectCard` ✅
- ~~`HeroActionButtons`~~ — 별도 컴포넌트로 분리하지 않음
- ~~`ResearchAreaCard`~~ → 홈에서는 인라인 처리
- ~~`BoardPostListItem`~~ — Phase 2
- ~~`MiniMemberProfileCard`~~ — `MemberCard` 재사용
- ~~`LocationMapCard`~~ — `ContactSummarySection` 내부에서 처리

핵심 재사용:

- `PublicationCard`, `ProjectCard`, `SectionHeader`는 다른 페이지에서도 그대로 사용 ✅

## Members 컴포넌트 ✅ (7개 구현)

### `/members` 교수 프로필 페이지

- `ProfessorProfile` ✅ (교수 전용 프로필 전체)

### `/members/students` 학생 목록 페이지

- `MemberGroupTabs` (client) ✅
- `MemberGrid` ✅
- `MemberCard` ✅

### `/members/[slug]` 멤버 상세 페이지

- `MemberEducationTimeline` ✅
- `MemberCareerTimeline` ✅
- `MemberContactLinks` ✅

### 미구현 (원본 설계에 있었으나 불필요)

- ~~`MembersHeroSection`~~ — `PageHero` 공통 컴포넌트로 대체
- ~~`SelectedMemberPanel`~~ — 상세 페이지(`/members/[slug]`)로 분리
- ~~`MemberProfileHero`~~ → `ProfessorProfile` 통합
- ~~`MemberIdentityBlock`~~ → `ProfessorProfile` 통합
- ~~`MemberResearchTags`~~ → 인라인 처리
- ~~`MemberSelectedPublications`~~ → 교차 링크로 대체
- ~~`MemberDetailHeader`~~ → 상세 페이지 내부 처리
- ~~`MemberSidebarProfile`~~ — 미구현
- ~~`MemberContentTabs`~~ — 미구현

핵심 재사용:

- `MemberCard` ✅
- `TagBadge` ✅

## Research `/research` 컴포넌트 ✅ (2개 구현)

### 구현된 컴포넌트

- `ResearchAreaDetailCard` ✅ (이름 변경: `ResearchAreaCard` →)
- `RelatedContentPreview` ✅ (통합: `RelatedProjectsPreview` + `RelatedPublicationsPreview` + `RelatedMembersPreview` →)

### 미구현

- ~~`ResearchHeroSection`~~ — `PageHero` 공통 컴포넌트로 대체
- ~~`ResearchAreaGridSection`~~ — 페이지 내부에서 직접 처리
- ~~`ResearchAreaSummary`~~ → `ResearchAreaDetailCard`에 통합

핵심 재사용:

- `SectionHeader` ✅

## Publications `/publications` 컴포넌트 ✅ (6개 구현)

### 구현된 컴포넌트

- `PublicationCard` ✅
- `PublicationList` ✅
- `PublicationFilters` (client) ✅ — 개별 필터(`TypeFilter`, `YearFilter` 등)를 통합
- `PublicationStatsPanel` ✅
- `PublicationExternalLinks` ✅ (이름 변경: `PublicationExternalLinksPanel` →)
- `BibtexCopyButton` (client) ✅

### 미구현 (원본 설계에 있었으나 불필요)

- ~~`PublicationsHeroSection`~~ — `PageHero` 공통 컴포넌트로 대체
- ~~`PublicationGridSection`~~ → `PublicationList` 통합
- ~~`PublicationPaginationSection`~~ → `Pagination` 공통 컴포넌트 사용
- ~~`PublicationAuthorList`~~ — 인라인 처리
- ~~`ReferenceCopyButton`~~ — `CopyButton` 공통 컴포넌트 사용
- 상세 페이지 세부 컴포넌트들 → 페이지 내부에서 직접 처리

핵심 재사용:

- `PublicationCard` ✅
- `CopyButton` ✅
- `TagBadge` ✅
- `ExternalLinkButton` ✅

## Projects `/projects` 컴포넌트 ✅ (3개 구현)

### 구현된 컴포넌트

- `ProjectCard` ✅
- `ProjectGrid` ✅
- `ProjectFilters` (client) ✅ — 개별 필터(`StatusFilter`, `CategoryFilter` 등)를 통합

### 미구현 (원본 설계에 있었으나 불필요)

- ~~`ProjectsHeroSection`~~ — `PageHero` 공통 컴포넌트로 대체
- ~~`ProjectCategoryPanel`~~ → `ProjectFilters`에 통합
- ~~`ProjectSummaryBar`~~ → 페이지 내부에서 직접 처리
- ~~`ProjectGridSection`~~ → `ProjectGrid` 통합
- ~~`ProjectPaginationSection`~~ → `Pagination` 공통 컴포넌트 사용
- ~~`ProjectCategoryCard`~~ — 미구현
- ~~`ProjectMetaRows`~~ — 인라인 처리
- ~~`ProjectTagList`~~ — 인라인 처리
- 상세 페이지 세부 컴포넌트들 → 페이지 내부에서 직접 처리

핵심 재사용:

- `ProjectCard` ✅
- `StatusBadge` ✅
- `CategoryBadge` ✅

## Board `/board` 컴포넌트 — Phase 2 예정

### 목록 페이지 조합

- `BoardHeroSection`
- `BoardSearchSection`
- `BoardCategoryTabs`
- `BoardEntryCount`
- `BoardListSection`

### 세부 컴포넌트

- `BoardPostListItem`
- `BoardCategoryBadge`
- `PinnedPostIndicator`
- `BoardDateLabel`

### 상세 페이지 `/board/[slug]`

- `BoardPostHeader`
- `BoardPostMeta`
- `BoardPostContent`
- `BoardAttachmentsSection`
- `BoardRelatedLinksSection`
- `RelatedProjectsSection`
- `RelatedPublicationsSection`

핵심 재사용:

- `BoardPostListItem`
- `SearchInput`
- `CategoryBadge`

## Live Demo `/live-demo` 컴포넌트 — Phase 2 예정

### 목록 페이지 조합

- `LiveDemoHeroSection`
- `LiveDemoSearchSection`
- `LiveDemoSidebarMenu`
- `LiveDemoGridSection`

### 세부 컴포넌트

- `DemoCategoryTree`
- `DemoCard`
- `DemoVisibilityBadge`
- `DemoLaunchButton`

### 상세 페이지 `/live-demo/[slug]`

- `DemoDetailHeader`
- `DemoSummaryPanel`
- `DemoAccessConditionSection`
- `DemoLaunchSection`
- `DemoDocumentationLinks`
- `RelatedProjectSection`

핵심 재사용:

- `DemoCard`
- `StatusBadge`
- `ExternalLinkButton`

## Contact `/contact` 컴포넌트 ✅ (3개 구현)

### 구현된 컴포넌트

- `ContactInfoCard` ✅ (통합: `ContactInfoSection` + `ContactInfoList` →)
- `MapSection` ✅ (이름 변경: `MapEmbedCard` →)
- `DirectionsSection` ✅

### 미구현

- ~~`ContactHeroSection`~~ — `PageHero` 공통 컴포넌트로 대체
- ~~`LabLocationSection`~~ → `ContactInfoCard`에 통합
- ~~`InquirySection`~~ — 미구현
- ~~`InquiryActionButtons`~~ — 미구현

## 인증 페이지 컴포넌트

### Login `/login`

- `LoginPageLayout`
- `LoginCard`
- `OAuthLoginButtons`
- `EmailLoginForm`

### Account `/account`

- `AccountHeader`
- `AccountProfileSection`
- `AccountRoleBadge`
- `AccountSecuritySection`
- `AccountLinkedMemberSection`

## 공통 재사용 패턴

### 카드 패턴

- `PublicationCard`, `ProjectCard`, `DemoCard`, `MemberCard`는 모두 다음 구조를 공유한다.
  - 상단 메타 영역
  - 제목
  - 요약 또는 핵심 정보
  - 태그 / 상태
  - 하단 액션

### 필터 패턴

- `Projects`, `Publications`, `Patents`는 동일한 검색 / 필터 / 개수 표시 패턴을 유지한다. ✅
- `Board`, `Live Demo`는 Phase 2에서 동일 패턴 적용 예정.
- 상단에는 검색창
- 그 아래에는 필터 또는 카테고리
- 그 아래에는 엔트리 수와 현재 필터 상태

### 상세 페이지 패턴

- 모든 상세 페이지는 기본적으로 다음 구조를 공유한다.
  - `Header`
  - `MetaPanel`
  - `BodySection`
  - `RelatedContentSection`

## 구현된 폴더 구조 ✅

실제 구현:

- `components/layout/` — SiteHeader, SiteFooter, Container, MainNavigation, MobileNavigationDrawer ✅
- `components/shared/` — SearchInput, FilterBar, FilterChips, Pagination, EmptyState, SectionHeader, PageHero, TagBadge, StatusBadge, CategoryBadge, StatCard, ExternalLinkButton, CopyButton ✅
- `components/home/` — 9개 홈 섹션 컴포넌트 ✅
- `components/members/` — MemberGroupTabs, ProfessorProfile, MemberCard, MemberGrid, MemberEducationTimeline, MemberCareerTimeline, MemberContactLinks ✅
- `components/research/` — ResearchAreaDetailCard, RelatedContentPreview ✅
- `components/publications/` — PublicationCard, PublicationList, PublicationFilters, PublicationStatsPanel, PublicationExternalLinks, BibtexCopyButton ✅
- `components/projects/` — ProjectCard, ProjectGrid, ProjectFilters ✅
- `components/patents/` — PatentCard ✅
- `components/contact/` — ContactInfoCard, MapSection, DirectionsSection ✅
- `components/board/` — Phase 2 예정
- `components/demos/` — Phase 2 예정
- `components/auth/` — Phase 2 예정

> 참고: `components/navigation/`은 `components/layout/`에 통합됨

원칙:

- 페이지 이름 기준으로 묶되, 완전히 공통인 것은 `shared`에 둔다. ✅
- 내부 포털 컴포넌트는 별도 `components/internal/`로 분리하는 것이 좋다.
